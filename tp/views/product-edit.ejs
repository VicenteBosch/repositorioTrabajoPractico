<!DOCTYPE html>
<html lang="es">
	<!-- Include Head -->
	<%- include('./partials/head') %>

	<body>
		<!-- Header -->
        <% if (locals.user) { %>

			<%- include('./partials/headerLogueado') %>

	  
		<% } else { %> 

				<%- include('./partials/header') %>

		<% }  %> 
		<!-- /Header -->

		<!-- Novedades -->
		<div class="container products-wrapper">
			<div class="row">
				<div class="col-12">
					<h2 class="products-title">Editar mi producto</h2>
				</div>
				<!-- Show the "ultimos" products -->
				<form action="/products/edit/<%= auto.id_producto %>"  method="POST" novalidate>
					<ul>

						<li><label for="fotoNueva"> Foto del producto</label></li>
						<li class = "formularios"><input type="file" id="fotoNuevo" name="fotoNuevo"
							value="<%= locals.oldData && oldData.fotoNuevo ? oldData.fotoNuevo : null %>"></li>

                        <% if (locals.errors && errors.fotoNuevo) { %>
                            <div class="error">
                                <p><%= errors.fotoNuevo.msg %></p>
                            </div>
                        <% } %>
						
						<li><label for="nombreNuevo"> Nombre del producto</label></li>
						<li class = "formularios"><input type="text" id="nombreNuevo" name="nombreNuevo"
							value="<%= locals.oldData && oldData.nombreNuevo ? oldData.nombreNuevo : null %>"></li>

						<% if (locals.errors && errors.nombreNuevo) { %>
                            <div class="error">
                                <p><%= errors.nombreNuevo.msg %></p>
                            </div>
                        <% } %>

						<li><label for="descripcionNuevo">Descripcion del producto</label></li>
						<li class = "formularios"><input type="text" id="descripcionNuevo" name="descripcionNuevo"
							value="<%= locals.oldData && oldData.descripcionNuevo ? oldData.descripcionNuevo : null %>"></li>
						
                        <% if (locals.errors && errors.descripcionNuevo) { %>
                            <div class="error">
                                <p><%= errors.descripcionNuevo.msg %></p>
                            </div>
                        <% } %>

                        <button class="bottom" type="submit">Guardar cambios</button>

                        
					</ul>
				</form>
			</div>
		</div>
		<!-- /Novedades -->

		<!-- footer -->
		<%- include('./partials/footer') %>
		<!-- /footer -->
	</body>
</html>
